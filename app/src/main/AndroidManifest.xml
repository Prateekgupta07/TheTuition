<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thetuition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher244235453"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".practicepap7"></activity>
        <activity android:name=".testpap8" />
        <activity android:name=".PDFopenert7" />
        <activity android:name=".testpap7" />
        <activity android:name=".PDFopenerp8" />
        <activity android:name=".practicepap8" />
        <activity android:name=".pdf" />
        <activity android:name=".PDFopenerp7" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
